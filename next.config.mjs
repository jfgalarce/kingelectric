/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Para que genere archivos estáticos compatibles
  distDir: 'out',   // Cambia la carpeta de salida a `out`

};

export default nextConfig;
